# This file was produced by the following command:
# conda env export --from-history --name preprocessing > preprocessing4.yml && echo "# Platform $(lsb_release -d)" >> preprocessing4.yml
name: preprocessing
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - trimmomatic
  - bowtie2
prefix: /BIOS-Share/home/jeperezj/miniconda3/envs/preprocessing
# Platform Description:	CentOS release 6.8 (Final)
