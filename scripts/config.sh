#!/bin/sh
# Configuration info created at install
# install.sh writes into this file, uninstall.sh reads from it

COMPLETION_DIR=${BASH_COMPLETION_USER_DIR:-${XDG_DATA_HOME:-$HOME/.local/share}/bash-completion}/completions
